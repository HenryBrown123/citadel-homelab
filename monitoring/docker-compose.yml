services:
  uptime-kuma:
    image: louislam/uptime-kuma:1
    container_name: citadel-monitor
    restart: unless-stopped
    volumes:
      - uptime-kuma-data:/app/data

volumes:
  uptime-kuma-data:
